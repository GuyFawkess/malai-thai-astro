---
import { useTranslations } from "../i18n/utils";

const { currentLang = "en" } = Astro.props;
const t = useTranslations(currentLang);
---

<section
  id="about"
  class="w-full py-12 px-4 bg-[var(--color-warm-sand)] text-center shadow-sm"
>
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl font-semibold mb-8 text-[var(--color-terracotta)]">
      {t.about.title}
    </h2>
    <div class="flex flex-col md:flex-row gap-8 items-center">
      <img
        data-duration="1000"
        data-animate="fade-left"
        loading="lazy"
        decoding="async"
        src="/aboutPic.webp"
        alt="Thai Massage Lanzarote - Traditional Thai massage techniques"
        width="600"
        height="400"
        class="opacity-0 -translate-x-10 rounded-lg hidden md:block w-full md:w-1/2 mr-7"
      />

      <div
        data-duration="1000"
        data-animate="fade-up"
        class="opacity-0 translate-y-10 w-full md:w-1/2 text-left"
      >
        <h3 class="text-xl font-bold mb-4 text-[var(--color-atention)]">
          {t.about.subtitle}
        </h3>

        <p class="mb-4 leading-relaxed">
          {t.about.intro}
        </p>

        <p class="mb-4 leading-relaxed">
          {t.about.mission}
        </p>

        <p class="mb-4 leading-relaxed">{t.about.services}</p>

        <ul class="list-disc pl-6 mb-4">
          {t.about.servicesList.map((service) => <li>{service}</li>)}
        </ul>

        <p class="leading-relaxed">
          {t.about.benefits}
        </p>

        <p class="mt-4 italic">
          {t.about.closing}
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  p,
  li {
    color: var(--color-deep-charcoal);
  }
</style>
